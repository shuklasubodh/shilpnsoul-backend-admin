import{Router}from'express';import sql from'./db.js';import{authenticate,isAdmin}from'./auth.js';import{notFound}from'./utils.js';
const router=Router();router.use(authenticate);
router.post('/carts',async(req,res)=>{const rows=await sql`INSERT INTO carts(user_id)VALUES(${req.user.id})RETURNING *`;return res.status(201).json(rows[0])});
router.get('/carts',async(req,res)=>{const rows=isAdmin(req.user)?await sql`SELECT * FROM carts ORDER BY id DESC`:await sql`SELECT * FROM carts WHERE user_id=${req.user.id} ORDER BY id DESC`;res.set('X-Total-Count',rows.length);return res.json(rows)});
router.get('/carts/:id',async(req,res)=>{const cart=(await sql`SELECT * FROM carts WHERE id=${req.params.id}`)[0];if(!cart||!isAdmin(req.user)&&String(cart.user_id)!==String(req.user.id))return notFound(res,'Cart');cart.items=await sql`SELECT ci.*,p.name product_name,p.image_url FROM cart_items ci JOIN products p ON p.id=ci.product_id WHERE ci.cart_id=${req.params.id}`;return res.json(cart)});
router.delete('/carts/:id',async(req,res)=>{const rows=isAdmin(req.user)?await sql`DELETE FROM carts WHERE id=${req.params.id} RETURNING *`:await sql`DELETE FROM carts WHERE id=${req.params.id} AND user_id=${req.user.id} RETURNING *`;return rows[0]?res.json(rows[0]):notFound(res,'Cart')});
router.post('/cart-items',async(req,res)=>{const{cart_id,product_id}=req.body,quantity=Number(req.body.quantity),cart=(await sql`SELECT * FROM carts WHERE id=${cart_id}`)[0],product=(await sql`SELECT * FROM products WHERE id=${product_id} AND is_active=true`)[0];if(!cart||!isAdmin(req.user)&&String(cart.user_id)!==String(req.user.id)||!product||!Number.isInteger(quantity)||quantity<1||quantity>product.stock_quantity)return res.status(400).json({error:'Invalid cart item.'});const subtotal=(Number(product.price)*quantity).toFixed(2),rows=await sql`INSERT INTO cart_items(cart_id,product_id,quantity,unit_price,subtotal)VALUES(${cart_id},${product_id},${quantity},${product.price},${subtotal})RETURNING *`;return res.status(201).json(rows[0])});
router.put('/cart-items/:id',async(req,res)=>{const quantity=Number(req.body.quantity),old=(await sql`SELECT ci.*,c.user_id,p.stock_quantity FROM cart_items ci JOIN carts c ON c.id=ci.cart_id JOIN products p ON p.id=ci.product_id WHERE ci.id=${req.params.id}`)[0];if(!old||!isAdmin(req.user)&&String(old.user_id)!==String(req.user.id)||!Number.isInteger(quantity)||quantity<1||quantity>old.stock_quantity)return res.status(400).json({error:'Invalid cart item.'});const rows=await sql`UPDATE cart_items SET quantity=${quantity},subtotal=${(Number(old.unit_price)*quantity).toFixed(2)},updated_at=NOW() WHERE id=${req.params.id} RETURNING *`;return res.json(rows[0])});
router.delete('/cart-items/:id',async(req,res)=>{const old=(await sql`SELECT ci.*,c.user_id FROM cart_items ci JOIN carts c ON c.id=ci.cart_id WHERE ci.id=${req.params.id}`)[0];if(!old||!isAdmin(req.user)&&String(old.user_id)!==String(req.user.id))return notFound(res,'Cart item');const rows=await sql`DELETE FROM cart_items WHERE id=${req.params.id} RETURNING *`;return res.json(rows[0])});
export default router;
