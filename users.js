import{Router}from'express';import sql from'./db.js';import{authenticate,admin,isAdmin,hashPassword}from'./auth.js';import{emailPattern,notFound,page,userColumns}from'./utils.js';
const router=Router();router.use(authenticate);
router.post('/',admin,async(req,res)=>{const{first_name,last_name,phone=null}=req.body,email=String(req.body.email||'').trim().toLowerCase(),password=String(req.body.password||''),role=String(req.body.role||'CUSTOMER').toUpperCase(),active=req.body.is_active??true;if(!first_name||!last_name||!emailPattern.test(email)||password.length<12||!['ADMIN','CUSTOMER'].includes(role)||typeof active!=='boolean')return res.status(400).json({error:'Invalid user fields.'});const hash=await hashPassword(password);const rows=await sql`INSERT INTO users(first_name,last_name,email,password_hash,phone,role,is_active)VALUES(${first_name},${last_name},${email},${hash},${phone},${role},${active})RETURNING id,first_name,last_name,email,phone,role,is_active,created_at,updated_at`;return res.status(201).json(rows[0])});
router.get('/',admin,async(req,res)=>{const p=page(req.query),count=await sql`SELECT COUNT(*)::int count FROM users`,rows=await sql.query(`SELECT ${userColumns} FROM users ORDER BY id LIMIT $1 OFFSET $2`,[p.limit,p.start]);res.set('X-Total-Count',count[0].count);return res.json(rows)});
router.get('/:id',async(req,res)=>{if(!isAdmin(req.user)&&String(req.user.id)!==req.params.id)return res.status(403).json({error:'Access denied.'});const rows=await sql.query(`SELECT ${userColumns} FROM users WHERE id=$1`,[req.params.id]);return rows[0]?res.json(rows[0]):notFound(res,'User')});
router.put('/:id',async(req,res)=>{if(!isAdmin(req.user)&&String(req.user.id)!==req.params.id)return res.status(403).json({error:'Access denied.'});const old=(await sql`SELECT * FROM users WHERE id=${req.params.id}`)[0];if(!old)return notFound(res,'User');const first=req.body.first_name??old.first_name,last=req.body.last_name??old.last_name,email=String(req.body.email??old.email).trim().toLowerCase(),phone=req.body.phone===undefined?old.phone:req.body.phone,role=isAdmin(req.user)?String(req.body.role??old.role).toUpperCase():old.role,active=isAdmin(req.user)?req.body.is_active??old.is_active:old.is_active;let hash=old.password_hash;if(req.body.password!==undefined){if(typeof req.body.password!=='string'||req.body.password.length<12)return res.status(400).json({error:'Password requires at least 12 characters.'});hash=await hashPassword(req.body.password)}const rows=await sql.query(`UPDATE users SET first_name=$1,last_name=$2,email=$3,phone=$4,role=$5,is_active=$6,password_hash=$7,updated_at=NOW() WHERE id=$8 RETURNING ${userColumns}`,[first,last,email,phone,role,active,hash,req.params.id]);return res.json(rows[0])});
router.delete('/:id',admin,async(req,res)=>{if(String(req.user.id)===req.params.id)return res.status(409).json({error:'Cannot delete yourself.'});const rows=await sql.query(`DELETE FROM users WHERE id=$1 RETURNING ${userColumns}`,[req.params.id]);return rows[0]?res.json(rows[0]):notFound(res,'User')});
export default router;
